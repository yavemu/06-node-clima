# API para obtener la temperatura de una dirección (Ubicación) deseada.

Al descargar del repositorio ejecutar 
```
npm install
```

### Ejecutar desde la consolta 
```
node app -descripcion [-d] "Ubicacion"
```